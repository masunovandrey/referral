{
  "name": "referral",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run tests/unit",
    "test:rules": "firebase emulators:exec --project demo-referral --only firestore \"vitest run tests/rules\""
  },
  "dependencies": {
    "firebase": "^10.14.1",
    "firebaseui": "^6.1.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "firebase-tools": "^13.35.1",
    "jsdom": "^26.0.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
